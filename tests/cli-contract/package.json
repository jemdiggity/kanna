{
  "name": "@kanna/cli-contract",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "sh -c 'if [ \"$1\" = \"--\" ]; then shift; fi; exec vitest run \"$@\"' --"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
